# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
dist-voice-demo
*.local

# Backend on-disk uploads (binary content; metadata lives in process memory)
backend/uploads/
backend/recordings/

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Claude Code local permissions (per-machine)
.claude/settings.local.json

# Phase 5 验证脚本的一次性临时数据库
.tmp-phase5/
.tmp-sysroom/
backend/.tmp-test/
.tmp-rooms/
.tmp-presence/
.tmp-reading/

# ── 仓库只放当前项目本身 ────────────────────────────────────
# 以下都留在本地磁盘上，只是不进仓库：

# 2025-11 ~ 2026-02 的旧 AI 对话记录，32 MB，与 App 无关
migrated_prompt_history/

# 一次性验收报告（结论已写进 docs/PRAYER_ROOM_SEC2_REPORT.md）
sec2-report.txt

# 早期把组件源码抄成 Markdown 的存档，984 行，真实代码在 components/ 里
动画与首页UI代码.md

# 无处引用的散图（代码里 0 处引用）
aa777956-a567-4b53-a1c6-1bc7ad78e71a.png

# 历史重写前的 .git 备份（59MB）。是本地保险，不进版本控制。
.git-backup-before-history-rewrite/
